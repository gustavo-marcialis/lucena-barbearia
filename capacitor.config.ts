import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.barbearialucena.app',
  appName: 'Barbearia Lucena',
  webDir: 'out'
};

export default config;
